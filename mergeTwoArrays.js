function mergeArrays(a, b) {
    let mergedArrays = [],
        longest = a.length > b.length ? a:b;
    for (let i=0; i<longest.length; i++){
      if (a[i]) j.push(a[i])
      if (b[i]) j.push(b[i])
    }
    return mergedArrays
  }